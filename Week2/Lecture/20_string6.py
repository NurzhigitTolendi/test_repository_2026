# Strings

# items in a string separated by a space

items = "cat was chasing a rat in a fast car"

print(items.split()) # string is split into separate strings
# we get a list of splitted string as a result
# the separator is the space by default (' ')

items_list = items.split()

print(items_list)