import os

new_folder_name = 'new_folder'

os.mkdir(new_folder_name) # using relative path
# mkdir - make directory
# realtive path - the folder will be created
# in the directory where the terminal is currently at
# that is, current working directory